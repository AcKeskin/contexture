[project]
name = "mcp-lib"
version = "0.1.0"
description = "Shared utilities for MCP API-wrapper servers — fetch, rate limiting, error formatting"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27.0"]
